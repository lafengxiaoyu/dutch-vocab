# Server port
server.port=${PORT:8080}

# MongoDB Atlas - Using full connection string from environment variable
spring.data.mongodb.uri=mongodb+srv://${MONGO_USERNAME}:${MONGO_PASSWORD}@mymongo.wsf3b27.mongodb.net/dutch-vocab?retryWrites=true&w=majority&tls=true&tlsAllowInvalidHostnames=true
spring.data.mongodb.auto-index-creation=true

# Connection pool
spring.data.mongodb.connection-pool.max-size=20
spring.data.mongodb.connection-pool.min-size=5

# Logging
logging.level.org.springframework.data=INFO
logging.level.com.dutch_vocab=INFO